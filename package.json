{
  "name": "erp-system",
  "version": "0.1.0",
  "private": true,
  "description": "ERP modular y componible (proyecto de estadía — UPTx)",
  "workspaces": [
    "client",
    "server",
    "packages/shared"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "dev:web": "npm run dev:web --workspace=client",
    "test": "npm test --workspace=server"
  },
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "react-native": "0.86.3"
  }
}
